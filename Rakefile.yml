---
changelog:
  git_repository: github.com/4-20ma

style:
  foodcritic:
    fail_tags:
      - any
    include_rules:
      - 'spec/foodcritic'
    # exclude tags by using ~FC002 notation within :tags array
    tags: []
